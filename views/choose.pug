extends layout

block content
  h1 Choose
  - let image_names = ["Norway.jpg","Paris.jpg","Dubai.jpg","Paris1.jpg","Dubai1.jpg"]
   -for(let i=0; i<3; i++){
    -let random= Math.floor(Math.random()*5)
     img(src=`/images/${image_names[random]}` alt=`Image Name journey${random}` height="300px" width="300px")
    -}